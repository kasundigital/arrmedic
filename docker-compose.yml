services:
  arrmedic:
    image: ghcr.io/kasundigital/arrmedic:latest
    container_name: arrmedic
    ports:
      - "7080:7080"
    volumes:
      - ./config:/config
      # Add your media root later when filesystem diagnostics are enabled.
      # - /mnt/media:/media:ro
    restart: unless-stopped
